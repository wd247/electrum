#!/usr/bin/env python3
# -*- mode: python -*-
#
# Electrum-LTM - 간단한 실행 스크립트
# LTM (Laptop Mining) 코인용 Electrum 지갑
#

import os
import sys

# LTM 네트워크 정보 출력
def show_ltm_info():
    print("🚀 Electrum-LTM (Laptop Mining Coin Wallet)")
    print("=" * 50)
    print("📋 LTM 네트워크 정보:")
    print(f"   제네시스 블록: 20a1cb14930e9cc8f0b7e6872b0630a86c135a6903aec70b6c4e63457c7948a8")
    print(f"   제네시스 메시지: David project begins")  
    print(f"   블록 시간: 1분 (적응형 난이도)")
    print(f"   알고리즘: SHA256d")
    print(f"   서버: ltm-wallet.gnc.ne.kr:50008,50009")
    print("=" * 50)

# LTM 네트워크를 기본으로 설정하고 원본 run_electrum 실행
if not any(arg.startswith('--') and ('mainnet' in arg or 'testnet' in arg or 'regtest' in arg or 'signet' in arg or 'ltm' in arg) for arg in sys.argv):
    sys.argv.append('--ltm')

# 시작 정보 표시
show_ltm_info()
print("� LTM 네트워크로 Electrum 시작 중...")
print()

# 원본 run_electrum 실행
script_dir = os.path.dirname(os.path.realpath(__file__))
sys.path.insert(0, script_dir)

if __name__ == '__main__':
    exec(open(os.path.join(script_dir, 'run_electrum')).read())